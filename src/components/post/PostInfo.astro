---
import { PostInfoProps } from "src/util/props.types";

const { tags, next, prev } = Astro.props as PostInfoProps
---

<div class="divide-y">
    <div class="mb-8">
        <h2 class="text-2xl font-magiel py-2">Tags:</h2>
        <ul class="gap-2 mt-2 text-xs uppercase flex flex-wrap">
            {
            [...tags, ...tags, ...tags].sort(() => Math.random() - .5).map((tag) => (
            <li class="my-1 shadow-2xl">
                <a href={`/tag/${tag.replace("/", "")}`} class="py-1 px-2 rounded-full bg-skin-main-accent text-skin-inverted">
                    {tag}
                </a>
            </li>
            ))}
        </ul>
    </div>
    <div class="py-4 flex md:flex-col justify-between">
        {next && <a class="text-lg text-skin-accent" href={`/blog/${next}`}>Go to next post</a>}
        {prev && <a class="text-lg text-skin-accent" href={`/blog/${prev}`}>Go to previous post</a>}
    </div>
</div>